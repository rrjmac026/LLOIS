<Window x:Class="LLOIS.Views.AddCommitteeReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Committee Report" Height="760" Width="640"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BgPrimaryBrush}">

    <Window.Resources>

        <Thickness x:Key="SectionGap">0,22,0,0</Thickness>
        <Thickness x:Key="FieldGap">0,14,0,0</Thickness>
        <Thickness x:Key="LabelGap">0,0,0,5</Thickness>

        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="{StaticResource LabelGap}"/>
        </Style>

        <Style TargetType="Run" x:Key="ReqMark">
            <Setter Property="Foreground" Value="{DynamicResource DangerFgBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SectionTitle">
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionSubtitle">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}"/>
            <Setter Property="Margin" Value="0,1,0,0"/>
        </Style>

        <Style TargetType="Border" x:Key="SectionCard">
            <Setter Property="Background" Value="{DynamicResource BgSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderThinBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="18,16"/>
        </Style>

        <Style TargetType="Border" x:Key="IconBadge">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="{DynamicResource BrandVeryLightBrush}"/>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderMedBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource BgHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{DynamicResource BrandBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <DockPanel Margin="0">

        <Border DockPanel.Dock="Top"
                Background="{DynamicResource BrandBrush}"
                Padding="22,16">
            <StackPanel>
                <TextBlock x:Name="WindowTitle" Text="Add Committee Report"
                           Foreground="White" FontSize="17" FontWeight="Bold"/>
                <TextBlock Text="Fill in the details below. Fields marked with * are required."
                           Foreground="#C8DCFF" FontSize="11.5" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource BgSecondaryBrush}"
                BorderBrush="{DynamicResource BorderThinBrush}"
                BorderThickness="0,1,0,0"
                Padding="22,14">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left"
                           Text="* Required fields"
                           FontSize="11"
                           Foreground="{DynamicResource TextTertiaryBrush}"
                           VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelBtn" Content="Cancel" Width="92" Margin="0,0,10,0"
                            Style="{StaticResource SecondaryBtn}" Click="CancelBtn_Click"/>
                    <Button x:Name="SaveBtn" Content="💾  Save Report" Width="150"
                            Style="{StaticResource PrimaryBtn}" Click="SaveBtn_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="22,18,22,18">

                <Border x:Name="ErrorBanner"
                        Background="{DynamicResource DangerBgBrush}"
                        BorderBrush="{DynamicResource DangerBorderBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="12,10"
                        Margin="0,0,0,16" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠" Foreground="{DynamicResource DangerFgBrush}"
                                   FontSize="14" Margin="0,0,8,0" VerticalAlignment="Top"/>
                        <TextBlock x:Name="ErrorText"
                                   Foreground="{DynamicResource DangerFgBrush}"
                                   TextWrapping="Wrap" FontSize="12.5"/>
                    </StackPanel>
                </Border>

                <!-- ── Section: Report Identity ─────────────────────── -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <Border Style="{StaticResource IconBadge}">
                                <TextBlock Text="🗂" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Report Identity" Style="{StaticResource SectionTitle}"/>
                                <TextBlock Text="Core identifying information" Style="{StaticResource SectionSubtitle}"/>
                            </StackPanel>
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource Label}">
                                    Report Number <Run Text="*" Style="{StaticResource ReqMark}"/>
                                </TextBlock>
                                <TextBox x:Name="ReportNumberBox" Height="34"
                                         Style="{StaticResource PlaceholderTextBox}"
                                         Tag="e.g. CR-2024-001"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource Label}">
                                    Date <Run Text="*" Style="{StaticResource ReqMark}"/>
                                </TextBlock>
                                <DatePicker x:Name="DatePickerControl" Height="34"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="{StaticResource FieldGap}">
                            <TextBlock Style="{StaticResource Label}">
                                Subject <Run Text="*" Style="{StaticResource ReqMark}"/>
                            </TextBlock>
                            <TextBox x:Name="SubjectBox" Height="76"
                                     Style="{StaticResource PlaceholderTextBox}"
                                     Tag="What is this committee report about?"
                                     TextWrapping="Wrap" AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Padding="8,8"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ── Section: Authorship ───────────────────────────── -->
                <Border Style="{StaticResource SectionCard}" Margin="{StaticResource SectionGap}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <Border Style="{StaticResource IconBadge}">
                                <TextBlock Text="👤" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Authorship" Style="{StaticResource SectionTitle}"/>
                                <TextBlock Text="Who submitted and sponsored this report" Style="{StaticResource SectionSubtitle}"/>
                            </StackPanel>
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource Label}">
                                    Submitted By <Run Text="*" Style="{StaticResource ReqMark}"/>
                                </TextBlock>
                                <TextBox x:Name="SubmittedByBox" Height="34"
                                         Style="{StaticResource PlaceholderTextBox}"
                                         Tag="e.g. Committee on Finance"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource Label}">
                                    Sponsored By <Run Text="*" Style="{StaticResource ReqMark}"/>
                                </TextBlock>
                                <TextBox x:Name="SponsoredByBox" Height="34"
                                         Style="{StaticResource PlaceholderTextBox}"
                                         Tag="e.g. Hon. Juan dela Cruz"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ── Section: Attachments ──────────────────────────── -->
                <Border Style="{StaticResource SectionCard}" Margin="{StaticResource SectionGap}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <Border Style="{StaticResource IconBadge}">
                                <TextBlock Text="📎" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Attachments" Style="{StaticResource SectionTitle}"/>
                                <TextBlock Text="Attach any supporting files (optional)" Style="{StaticResource SectionSubtitle}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Content="📎 Add files…" Height="34" Width="130"
                                HorizontalAlignment="Left"
                                Style="{StaticResource SecondaryBtn}"
                                Click="AddFilesBtn_Click"/>

                        <ItemsControl x:Name="AttachmentsList" Margin="{StaticResource FieldGap}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderThinBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="10,8"
                                            Margin="0,0,0,6"
                                            Background="{DynamicResource BgPrimaryBrush}">
                                        <DockPanel>
                                            <Button DockPanel.Dock="Right"
                                                    Content="✕"
                                                    Width="26" Height="26"
                                                    Padding="0"
                                                    Foreground="{DynamicResource DangerFgBrush}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Click="RemoveAttachment_Click"
                                                    Tag="{Binding}"/>
                                            <TextBlock Text="{Binding FileName}"
                                                    VerticalAlignment="Center"
                                                    FontSize="12"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
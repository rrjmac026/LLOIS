<UserControl x:Class="LLOIS.Views.FeedbackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgPrimaryBrush}">

    <UserControl.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{DynamicResource BrandBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Border MaxWidth="620" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{DynamicResource BgSecondaryBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="28">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="6" Opacity="0.08" Color="#000000"/>
            </Border.Effect>

            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                    <Border Width="36" Height="36" CornerRadius="10"
                            Background="{DynamicResource BrandSoftBrush}"
                            Margin="0,0,12,0">
                        <TextBlock Text="💬" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel>
                        <TextBlock Text="Send Feedback" FontSize="22" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Share a bug, concern, or suggestion to help improve the system."
                                   FontSize="12.5" Foreground="{DynamicResource TextTertiaryBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Border x:Name="SuccessBanner"
                        Background="{DynamicResource AccentGreenLightBrush}"
                        BorderThickness="0" CornerRadius="10" Padding="12,10"
                        Margin="0,0,0,18" Visibility="Collapsed">
                    <TextBlock Text="✅ Feedback submitted. Thank you!"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="12.5" FontWeight="SemiBold"/>
                </Border>

                <TextBlock Text="Type" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                <ComboBox x:Name="TypeCombo" Height="38" SelectedIndex="0" Margin="0,0,0,16">
                    <ComboBoxItem Content="Bug"/>
                    <ComboBoxItem Content="Concern"/>
                    <ComboBoxItem Content="Suggestion"/>
                </ComboBox>

                <TextBlock Text="Message" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                <Border Background="{DynamicResource BgPrimaryBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                    <TextBox x:Name="MessageTextBox" Height="170"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="12,10"
                             Style="{StaticResource PlaceholderTextBox}"
                             Tag="Describe the bug, concern, or suggestion in detail..."
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                </Border>

                <Button x:Name="SubmitBtn" Content="Submit Feedback" Height="42"
                        Margin="0,20,0,0" Style="{StaticResource PrimaryBtn}"
                        Click="SubmitBtn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
<Window x:Class="LLOIS.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LLOIS" Height="150" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <StackPanel Margin="20" VerticalAlignment="Center">
        <TextBlock x:Name="StatusText" Text="Working..." FontSize="13" Margin="0,0,0,10"/>
        <ProgressBar x:Name="ProgressBarControl" Height="20" Minimum="0" Maximum="100"/>
        <TextBlock x:Name="PercentText" Text="0%" FontSize="11" Margin="0,6,0,0" HorizontalAlignment="Right"/>
    </StackPanel>
</Window>
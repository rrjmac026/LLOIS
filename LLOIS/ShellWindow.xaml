<Window x:Class="LLOIS.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LLOIS - Local Legislative Ordinance Information System"
        WindowState="Maximized"
        Background="White">

    <Window.Resources>
        <Style x:Key="FadeContent" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <ContentPresenter x:Name="presenter" Content="{TemplateBinding Content}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <ContentControl x:Name="ViewHost" Style="{StaticResource FadeContent}"/>
    </Grid>
</Window>